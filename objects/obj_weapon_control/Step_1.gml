/// @description Insert description here
// You can write your code in this editor
/*
if (keyboard_check(ord("3")) && selected != 0 && !cooldown)
	{
	with(obj_player.gun)
		{
		instance_destroy();
		}
	g = instance_create_depth(obj_player.x,obj_player.y,0,wpn[0])
	obj_player.gun = g;
	selected = 0;
	cooldown = true;
	alarm[0] = 60;
	}
else if (keyboard_check(ord("1")) && selected != 1 && !cooldown)
	{
	with(obj_player.gun)
		{
		instance_destroy();
		}
	g = instance_create_depth(obj_player.x,obj_player.y,0,wpn[1])
	obj_player.gun = g;
	selected = 1;
	cooldown = true;
	alarm[0] = 60;
	}
else if (keyboard_check(ord("2")) && selected != 2 && !cooldown)
	{
	with(obj_player.gun)
		{
		instance_destroy();
		}
	g = instance_create_depth(obj_player.x,obj_player.y,0,wpn[2])
	obj_player.gun = g;
	selected = 2;
	cooldown = true;
	alarm[0] = 60;
	}
	*/